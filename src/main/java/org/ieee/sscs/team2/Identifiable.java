package org.ieee.sscs.team2;

public interface Identifiable {
  /**
   * Every Object like user or car or payment that have unique id must have
   * getId() method to return its id
   */
  String getId();
}
